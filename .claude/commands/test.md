Run the test suite for the project. Follow these steps:

1. Detect the project type and test framework (look for package.json, pyproject.toml, Makefile, etc.)
2. Run the appropriate test command
3. If tests fail, analyze the failures and suggest fixes
4. Report a summary of passed/failed/skipped tests
